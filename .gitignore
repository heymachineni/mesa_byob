node_modules/
dist/
.astro/
.DS_Store
*.log
.env
.env.production

# Reference material, not part of the deliverable. The Starter Pack and design
# system are Mesa's internal documents and the newsletter is a third party's;
# the content they carry already lives in src/content/data/*.json.
*.docx
ginnis-post-newsletter.html

# Source assets, kept locally. What the site serves is derived from them and
# committed: public/fonts/*.woff2 (subset, 75 KB vs 6.3 MB of TTF) and
# public/mesa-mark.png (4 KB, extracted from the logo artwork).
Mesa fonts/
PG white.png

# Adapter build output
.vercel/
